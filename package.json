{
  "name": "monorepo",
  "private": true,
  "engines": {
    "node": ">=10",
    "yarn": "^1.17.3"
  },
  "workspaces": {
    "packages": ["packages/*"]
  },
  "scripts": {
    "lint": "wsrun -cs lint",
    "build": "wsrun -et -c build",
    "test": "wsrun -es -c test"
  },
  "dependencies": {
    "wsrun": "^5.2.1"
  }
}
